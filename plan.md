# Plan: task-mugukomq

**Project:** `PRJ-1952`  
**Task ID:** `task-mugukomq`  
**Repo:** `prj-5654-task-mugukomq`  
**Progress:** 1/5 subtasks done

## Summary

CLI-утилита на Python: парсинг аргументов, безопасный вызов git через subprocess, базовые команды (status/log/diff/branch/commit/push) с покрытием тестами и CI.

## Subtasks

### ❌ 1. Инициализировать структуру проекта

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Status:** `failed`
- **Description:** Создать репозиторий: pyproject.toml (с entry-point git-cli), .gitignore, requirements-dev.txt, директории src/git_cli/ и tests/. Выбрать argparse. Закоммитить initial scaffold в отдельной ветке.
- **Test plan:** pip install -e . проходит без ошибок; пакет git_cli импортируется; entry-point git-cli доступен через PATH.

### ⬜ 2. Реализовать безопасный subprocess-вызов git

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Status:** `pending`
- **Description:** Реализовать модуль runner.py: функция run_git(args, cwd, env, timeout) — список аргументов (НЕ shell=True), проверка кода возврата, возврат CompletedProcess, обработка GitNotInstalledError, GitCommandError, таймаут. Никаких shell=True и f-string в командах.
- **Test plan:** Тесты test_runner.py: успешный вызов (git --version), обработка non-zero exit с stderr, таймаут (sleep-эмуляция), GitNotInstalledError при PATH=/nonexistent, отсутствие shell-инъекций при `args=['log', '--format=%H', '; rm -rf /']`.
- **Dependencies:** `sub-1`

### ⬜ 3. Реализовать команды CLI (status/log/diff/branch)

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Status:** `pending`
- **Description:** Модуль commands.py с чистыми функциями: status(cwd), log(cwd, n=10), diff(cwd, staged=False), branch(cwd, list_all=False). Каждая возвращает структурированный dict/list, не сырой текст. Без subprocess внутри — использует runner.
- **Test plan:** Тесты test_commands.py на фикстуре tmp_git_repo (создаёт repo, делает 2 коммита): status возвращает dict с полями branch/clean/staged; log возвращает список из 2 элементов с hash/author/message; diff пустой для чистого repo; branch возвращает текущую ветку.
- **Dependencies:** `sub-2`

### ⬜ 4. Реализовать argparse-обёртку и entry-point

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Status:** `pending`
- **Description:** main.py с argparse: subparsers status/log/diff/branch/add/commit/push/clone. Маппинг subcommand → функции из commands. Красивый вывод (rich или plain text fallback). Вывод ошибок в stderr, exit codes (0/1/2/130).
- **Test plan:** CLI-смоук-тесты: `git-cli status` в фикстуре — exit 0 + ожидаемый stdout; `git-cli log -n 1` — exit 0 + 1 коммит; `git-cli commit -m test` после add — exit 0; `git-cli bogus` — exit 2 + usage в stderr.
- **Dependencies:** `sub-3`

### ⬜ 5. Покрыть тестами и настроить CI

- **ID:** `sub-5`
- **Profile:** `20razrab1`
- **Status:** `pending`
- **Description:** Довести coverage до >=85% для runner.py и commands.py. Добавить pytest в requirements-dev. Создать .github/workflows/ci.yml: matrix Python 3.10/3.11/3.12, шаги checkout/setup-python/install/test. Все тесты зелёные локально.
- **Test plan:** pytest --cov=git_cli --cov-fail-under=85 проходит; CI workflow валиден (act -l или push в feature-ветку триггерит job); отсутствие shell-injection в security-тестах; README с примерами вызова существует.
- **Dependencies:** `sub-4`


---
*Generated by Hermes Orchestrator*
